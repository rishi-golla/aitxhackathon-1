/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // appDir is enabled by default in Next.js 13+
  // No need for experimental flag in Next.js 16
};

module.exports = nextConfig;
